{
  "name": "lwc-slds-lbc",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf node_modules __lwr_cache__ site",
    "dev": "lwr dev",
    "dev:compat": "lwr dev --mode compat",
    "build": "lwr build --clean --mode prod-compat -o site",
    "build:prod-compat": "lwr build --clean --mode prod-compat -o site",
    "vercel-build": "lwr build --clean --mode prod-compat -o site",
    "start": "lwr start",
    "stage": "yarn build && yarn start",
    "stage:prod-compat": "yarn build:prod-compat && yarn start"
  },
  "dependencies": {
    "@salesforce-ux/design-system": "^2.26.2",
    "lightning-base-components": "^1.27.2-alpha",
    "lwc": "latest",
    "lwr": "0.18.1"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "volta": {
    "node": "20.16.0"
  }
}
